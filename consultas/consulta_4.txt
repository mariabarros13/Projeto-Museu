// analisando qual o tipo de ingresso mais vendido
// itens cumpridos: GROUP | SORT | LIMIT
db.ingressos.aggregate([
  {
    $group:{
      _id:"$tipoIngresso",
      quantidade:{
        $sum:1
      }
    }
  },

  {
    $sort:{
      quantidade:-1
    }
  },

  {
    $limit:1
  }
])